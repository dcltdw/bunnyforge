# Briefs

Per-session prep, one subdirectory per session (`Briefs/session-014/`) and one file per entity inside it. Holds only what is true THIS session; anything permanent belongs in the entity's own file. Use `_Templates/brief.md`.
